# create a docker-compose.yml file
version: "3"
services:
  web:
    container_name: one-team-web
    build:
      context: .
      dockerfile: ./deploy/Dockerfile
    ports:
      - "8006:80"
    external_links:
      - one-team-server_one-team-server_1:oneteamweb-back

